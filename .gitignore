# ── 빌드 산출물 ──
/release/
/MDeautify.exe
/MDeautify.zip
MDeautify-app/bin/
MDeautify-app/dist/

# ── 로컬 개발 설정 ──
.claude/

# ── 로그 ──
*.log

# ── 임시/상태 파일 ──
.tmp/
shots/
.storage/
**/.storage/
window_state.config.json

# ── 구버전 (Season3.5만 유지) ──
MDeautify_Season3.html

# ── 개인 파일 (저장소에 올리지 않음) ──
*.pptx
~$*.pptx
SKILL.md

# ── OS 부산물 ──
Thumbs.db
.DS_Store
desktop.ini
