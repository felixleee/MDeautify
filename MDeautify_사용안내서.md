---
제목: MDeautify 사용 안내서
부제: 마크다운(.md)을 깔끔한 PDF로 바꾸는 가장 쉬운 방법
kicker: MD2PDF USER GUIDE
버전: v1.4.0
작성일: 2026-08-12
대상: MDeautify 사용자
---

## A. MDeautify

**MDeautify**(부제 *md2pdf*)는 마크다운 문서(`.md`)를 **페이지 단위로 정돈된 PDF**로 만들어 주는 도구입니다. 

복잡한 설정이나 디자인 작업 없이, 평소처럼 마크다운으로 글을 쓰고 불러오기만 하면 됩니다. 표지부터 본문, 표, 코드 블록, 다이어그램까지 알아서 깔끔하게 배치해 줍니다.

이 안내서 또한 MDeautify로 만들어졌습니다. 즉, **여기 보이는 표지·제목·표·코드·다이어그램이 모두 "마크다운으로 작성하면 이렇게 나온다"를 보여 주는 실제 예시**입니다. 별도의 목업이나 예시 이미지가 아니라, 지금 읽고 계신 문서가 그대로 결과물인 셈입니다.

> **"마크다운만 던지면, 보고서 같은 PDF가 나옵니다."**


## B. 기본 사용 흐름

```mermaid
flowchart TD
A["MD 파일 열기<br>끌어다 놓기 · 버튼 · 더블클릭"] --> B["미리보기 확인<br>오른쪽에서 실제 PDF 모양을 바로 확인"]
B --> C["PDF 설정 조정<br>테마·글꼴·바닥글을 취향대로 변경"]
C --> D["PDF 저장<br>상단 저장 버튼 한 번"]
D --> E["완료<br>저장 위치만 지정하면 끝"]
```

1. **MD 파일 열기** — `.md` 파일을 창에 **끌어다 놓거나**(이미지가 있으면 이미지 파일과 함께, 또는 폴더째), `MD 파일 열기` 버튼으로 고릅니다. (`.md` 더블클릭으로 열어도 됩니다.)
2. **미리보기 확인** — 오른쪽에 실제 PDF 모양이 페이지 단위로 나타납니다.
3. **PDF 설정 조정** — 상단 `PDF 설정`에서 색상·글꼴·바닥글을 취향대로 바꿉니다.
4. **PDF 저장** — 상단 `PDF 저장`을 누르면 **미리보기 그대로 곧바로 PDF로 저장**됩니다. (자세히는 F절)

## C. PDF 설정 살펴보기

상단 **`PDF 설정`** 버튼을 누르면 아래 항목을 조정할 수 있습니다. (설정 창은 **X·바깥 클릭·Esc** 로 닫힙니다.)

| 설정 | 설명 |
|---|---|
| 색상 테마 | 프리셋 8종 + `직접 선택`(원하는 색). 제목·표 머리글·강조선 색이 문서 전체에 반영 |
| 본문 폰트 | Noto Sans / Pretendard 중 선택 |
| 기준 폰트 크기 | 10~20px. **문서 전체가 이 값에 비례**해 커지고 작아짐 |
| 쪽 바닥글 | 페이지 하단 문구. `{pageNumber}` `{totalPages}` 변수 사용 가능 |
| 쪽 머리말 | 페이지 상단 문구. `{title}` `{pageNumber}` `{totalPages}` 변수 사용 가능 (기본 꺼짐) |
| 제목(H2)마다 새 페이지 | 켜면 `##` 제목이 나올 때마다 새 페이지에서 시작 (기본 꺼짐) |
| 자동 목차(TOC) | 켜면 표지 다음에 제목(H1~H3)으로 목차를 자동 생성 — 점선 리더와 페이지 번호 포함 (기본 꺼짐) |
| 선택한 설정 기억하기 | 켜면 아래 설정들을 다음 실행 때도 기억 |

**"선택한 설정 기억하기"가 기억하는 범위**

- 기억함: 색상 테마 · 본문 폰트 · 기준 폰트 크기 · 쪽 바닥글 · 쪽 머리말 · 제목마다 새 페이지 · 자동 목차
- 항상 별개로 유지: **시스템 테마(다크)** (아래 설정(⚙) 메뉴의 토글이라 이 스위치와 무관하게 저장됨)

### 설정(⚙) 메뉴 — 자동 저장 · 시스템 테마

상단 오른쪽 **톱니(⚙)** 버튼에는 앱 전반 설정이 있습니다.

- **자동 저장** — 켜면 편집을 멈춘 뒤 미리보기가 갱신될 때 **원본 `.md` 파일에도 자동으로 저장**됩니다. (파일이 연결돼 있을 때만 동작 — G절 참고)
- **시스템 테마** — 화면 밝기(라이트/다크)를 시스템 설정에 맞춥니다.
- **업데이트 확인** — 새 버전이 있으면 앱 안에서 바로 받아 설치합니다.

## D. 지원하는 마크다운 표기법

여기서부터는 **"이렇게 쓰면(왼쪽 코드) → 이렇게 나온다(아래 결과)"** 형식으로 보여드립니다.

### 제목과 섹션 뱃지

제목은 `#` 개수로 크기가 정해집니다. 특히 **제목 앞에 `A.` `B.` 처럼 알파벳을 붙이면 번호 뱃지**가 붙습니다 (이 문서의 A·B·C… 처럼).

```
# 가장 큰 제목
## A. 알파벳을 붙이면 뱃지가 생겨요
### 소제목
```

### 강조 표기

```
**굵게**, *기울임*, ~~취소선~~, 그리고 `인라인 코드`
```

이렇게 나옵니다 → **굵게**, *기울임*, ~~취소선~~, 그리고 `인라인 코드`

> ⚠️ **취소선 주의**: 취소선은 물결 **두 개** `~~이렇게~~` 로 씁니다.
> 물결 **하나**는 숫자 범위(예: 166~169)로 보고 그대로 둡니다.

### 목록

```
- 순서 없는 항목
  - 들여쓰기로 하위 항목
1. 순서 있는 항목
2. 두 번째
```

- 순서 없는 항목
  - 들여쓰기로 하위 항목
1. 순서 있는 항목
2. 두 번째

### 표

```
| 이름 | 역할 |
|---|---|
| 홍길동 | 작성 |
| 김철수 | 검토 |
```

| 이름 | 역할 |
|---|---|
| 홍길동 | 작성 |
| 김철수 | 검토 |

> 팁: 표가 페이지를 넘어가면 **머리글이 다음 페이지에도 자동 반복**됩니다.

### 코드 블록

**언어 이름(`js`, `python`, `sql` 등)을 붙이면 색상 강조**가 적용됩니다. 언어 없이 그냥 ``` 로만 감싸면 단색으로 표시됩니다. (그래서 이 안내서의 "작성법 예시"는 단색, "실제 결과"는 색상으로 구분됩니다.)

**작성법 (이렇게 입력):**

````
```js
// 인사 함수
function hello(name) {
  const msg = "안녕, " + name;
  return 42;
}
```
````

**실제 결과 (이렇게 표시 — 주석·키워드·문자열·숫자·함수명이 색으로 구분):**

```js
// 인사 함수
function hello(name) {
  const msg = "안녕, " + name;
  return 42;
}
```

### 인용문과 구분선

```
> 이렇게 인용문을 씁니다.

---
```

> 이렇게 인용문을 씁니다.

---

### 링크

```
[MDeautify 안내](https://example.com)
```

[MDeautify 안내](https://example.com)

### 체크리스트 (할 일 목록)

`- [ ]` (빈 칸) / `- [x]` (체크됨) 으로 체크박스 목록을 만듭니다.

```
- [x] 끝난 일
- [ ] 남은 일
```

- [x] 끝난 일
- [ ] 남은 일

### 이미지

이미지를 넣는 방법은 세 가지입니다.

**① 붙여넣기 (가장 간편)** — 캡처하거나 복사한 이미지를 왼쪽 편집기에서 `Ctrl+V`. 그림이 **문서 안에 통째로 저장**되어, 파일을 옮기거나 다른 PC에서 열어도 깨지지 않습니다. 커서 위치에 자동으로 삽입되고, 페이지 폭보다 크면 **자동 축소**, 아주 큰 사진은 용량을 위해 **알아서 줄여** 담습니다.

**② 편집기에 끌어다 놓기** — 이미지 파일을 왼쪽 편집기의 원하는 위치에 끌어다 놓으면 **그 자리에 `![](파일명)` 참조가 자동 삽입**되고, 배지가 초록으로 깜빡이며 알려줍니다. (오른쪽 미리보기에 놓으면 본문엔 넣지 않고 아래 "불러온 파일" 목록에만 추가됩니다.)

**③ 같은 폴더의 이미지 파일 참조** — `.md` 옆(또는 하위 폴더)에 이미지를 두고 파일명으로 씁니다.

```
![소켓](socket.png)
![로고](images/logo.png)
```

이 방식은 다음 중 하나로 열 때 이미지가 표시됩니다: **①`.md`와 이미지 파일을 함께(또는 폴더째) 창에 끌어다 놓기**, 또는 **②`MD 파일 열기` 버튼 / `.md` 더블클릭.** (프로그램이 이미지 파일에 접근할 수 있어야 하므로, `.md` **하나만** 달랑 끌어다 놓으면 이미지는 표시되지 않습니다 — 이미지와 **함께** 넣거나 버튼/더블클릭으로 여세요.)

> 참고: 웹 주소 `![](https://...)` 형태도 사용할 수 있습니다.

**이미지 크기 조정** — 이미지 참조 뒤에 ` =가로x세로`를 붙이면 크기를 지정할 수 있습니다.

```
![로고](logo.png =300x200)   가로 300 · 세로 200 (px)
![로고](logo.png =300x)      가로 300, 세로는 비율 맞춰 자동
![로고](logo.png =x200)      세로 200, 가로는 자동
![로고](logo.png =300)       가로 300
![로고](logo.png =50%)       폭을 페이지의 50%로
```

크기를 지정하지 않아도, 세로로 아주 긴 이미지는 페이지 높이에 맞게 자동으로 줄어듭니다. (더 세밀하게는 `<img src="logo.png" width="300">` 처럼 HTML 태그를 직접 써도 됩니다.)

### 불러온 파일 확인 · ZIP으로 묶어 저장

편집기 위쪽의 **클립 아이콘**(불러온 파일 개수가 함께 표시됨)을 누르면 **현재 문서에 딸린 파일 목록**이 열립니다.

- 목록의 각 이미지에는 상태가 표시됩니다: **`✓ 사용중`**(본문에서 참조되어 표시됨) · **`● 사용가능`**(불러왔지만 아직 본문에 없음) · **`✗ 식별안됨`**(본문에 참조는 있으나 파일을 못 찾음).
- **`삽입`** 버튼(사용중·사용가능): 편집기 커서 위치에 `![](파일명)` 참조를 넣습니다.
- **`참조 삭제`** 버튼(식별안됨): 못 찾는 이미지의 참조를 본문에서 지웁니다.
- 목록 맨 위의 **`ZIP 저장`** 버튼을 누르면 **현재 MD 본문과 불러온 이미지 전체를 하나의 `.zip`** 으로 묶어 저장합니다. 압축을 풀어 폴더째 두고 다시 열면 이미지가 그대로 연결되므로, **문서와 이미지를 함께 보관하거나 다른 사람에게 전달할 때** 편리합니다.

### 다이어그램

`mermaid` 코드 블록으로 다이어그램을 그립니다. **총 10종을 지원**합니다 — 순서도(flowchart) · 시퀀스(sequenceDiagram) · ER(erDiagram) · 파이(pie) · 상태(stateDiagram) · 클래스(classDiagram) · 간트(gantt) · 사용자 여정(journey) · 마인드맵(mindmap) · 타임라인(timeline). **모든 다이어그램 색상은 선택한 테마에 자동으로 맞춰집니다.** (지원하지 않는 종류는 "지원하지 않는 다이어그램" 안내와 함께 원본 코드가 표시됩니다.)

**① 순서도 (flowchart)** — 방향(`TD` 세로 / `LR` 가로), 분기, 판단 마름모(`{ }`), 화살표 라벨을 지원합니다.

````
```mermaid
flowchart TD
A[시작] --> B{조건?}
B -->|예| C[처리]
B -->|아니오| D[종료]
C --> D
```
````

이렇게 그려집니다:

```mermaid
flowchart TD
A[시작] --> B{조건?}
B -->|예| C[처리]
B -->|아니오| D[종료]
C --> D
```

노드 모양: `[사각]` · `(둥근)` · `([스타디움])` · `{마름모}` · `((원))` / 방향은 첫 줄을 `flowchart LR` 로 바꾸면 가로로 흐릅니다.

**② 시퀀스 (sequenceDiagram)** — 참여자들이 주고받는 순서를 그립니다. `-->>` 는 점선(응답)입니다.

````
```mermaid
sequenceDiagram
사용자 ->> 앱: MD 파일 열기
앱 ->> 앱: 미리보기 렌더링
앱 -->> 사용자: PDF 저장
```
````

이렇게 그려집니다:

```mermaid
sequenceDiagram
사용자 ->> 앱: MD 파일 열기
앱 ->> 앱: 미리보기 렌더링
앱 -->> 사용자: PDF 저장
```

**③ ER 다이어그램 (테이블 관계)** — `||--o{` 같은 기호로 **관계의 수(카디널리티)** 를 나타냅니다. `||`=하나, `o{`=0개 이상(N), `|{`=1개 이상 → 예: `USER ||--o{ ORDER` 는 "회원 1명이 주문 여러 건".

````
```mermaid
erDiagram
USER ||--o{ ORDER : places
USER {
  int id PK
  string name
  string email
}
ORDER {
  int id PK
  int user_id FK
  date created
}
```
````

이렇게 그려집니다 (관계선 양끝에 `1` / `0..N` 표시):

```mermaid
erDiagram
USER ||--o{ ORDER : places
USER {
  int id PK
  string name
  string email
}
ORDER {
  int id PK
  int user_id FK
  date created
}
```

**④ 파이 차트 (pie)** — 비율을 원 그래프로. 조각·퍼센트·범례가 함께 그려집니다.

````
```mermaid
pie title 응답 비율
  "만족" : 62
  "보통" : 25
  "불만" : 13
```
````

```mermaid
pie title 응답 비율
  "만족" : 62
  "보통" : 25
  "불만" : 13
```

**⑤ 상태 다이어그램 (stateDiagram-v2)** — 상태 전이를 그립니다. `[*]` 는 시작·끝점입니다.

````
```mermaid
stateDiagram-v2
[*] --> 대기
대기 --> 처리중 : 시작
처리중 --> 완료 : 성공
처리중 --> 대기 : 실패
완료 --> [*]
```
````

```mermaid
stateDiagram-v2
[*] --> 대기
대기 --> 처리중 : 시작
처리중 --> 완료 : 성공
처리중 --> 대기 : 실패
완료 --> [*]
```

**⑥ 클래스 다이어그램 (classDiagram)** — 클래스의 속성·메서드와 관계(상속 `<|--`, 컴포지션 `*--`)를 그립니다.

````
```mermaid
classDiagram
class Document {
  +string title
  +render() void
}
Document <|-- Report
```
````

```mermaid
classDiagram
class Document {
  +string title
  +render() void
}
Document <|-- Report
```

**⑦ 간트 차트 (gantt)** — 일정을 표 형태로. 날짜 축·섹션·상태색(완료/진행/중요)·마일스톤을 지원합니다.

````
```mermaid
gantt
  title 프로젝트 일정
  dateFormat YYYY-MM-DD
  section 준비
  기획      :done, a1, 2026-08-01, 5d
  설계      :active, after a1, 4d
  section 개발
  구현      :crit, after a1, 10d
  출시      :milestone, 2026-08-20, 0d
```
````

```mermaid
gantt
  title 프로젝트 일정
  dateFormat YYYY-MM-DD
  section 준비
  기획      :done, a1, 2026-08-01, 5d
  설계      :active, after a1, 4d
  section 개발
  구현      :crit, after a1, 10d
  출시      :milestone, 2026-08-20, 0d
```

**⑧ 그 외 — 사용자 여정 · 마인드맵 · 타임라인**

- **journey**: 단계별 만족도 곡선. `작업명: 점수(1~5): 참여자` 형식.
- **mindmap**: 들여쓰기로 계층을 만드는 생각 지도. `root((중심))` 아래로 들여씁니다.
- **timeline**: 시간 순 사건 정리. `시점 : 사건 : 사건` 형식.

````
```mermaid
mindmap
  root((MDeautify))
    입력
      마크다운
    출력
      PDF
```
````

```mermaid
mindmap
  root((MDeautify))
    입력
      마크다운
    출력
      PDF
```

### 수식 (KaTeX)

`$ ... $` 로 감싸면 **문장 속 인라인 수식**, `$$ ... $$` 로 감싸면 **가운데 정렬 블록 수식**이 됩니다. 폰트가 내장되어 오프라인·PDF에서도 동일하게 표시됩니다.

```
질량-에너지 등가는 $E = mc^2$ 이고, 피타고라스 정리는 $a^2 + b^2 = c^2$ 입니다.

$$\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}$$
```

이렇게 나옵니다 → 질량-에너지 등가는 $E = mc^2$ 이고, 피타고라스 정리는 $a^2 + b^2 = c^2$ 입니다.

$$\int_0^\infty e^{-x^2}\,dx = \frac{\sqrt{\pi}}{2}$$

> 참고: `$5`, `$10` 처럼 뒤에 숫자가 붙은 통화 표기는 수식으로 인식하지 않고 그대로 둡니다.

### 강제 페이지 나눔

원하는 위치에서 페이지를 나누려면 그 줄에 아래 주석을 넣습니다. (다음 내용이 새 페이지에서 시작합니다.)

```
<!-- pagebreak -->
```

<!-- pagebreak -->

## E. 표지(커버) 만들기

문서 **맨 위**에 `---` 로 감싼 정보 블록을 넣으면 **표지 페이지**가 자동으로 만들어집니다. (이 문서의 첫 페이지처럼요.)

````
---
제목: 우리 회사 제안서
부제: 2026년 상반기
사업명: 신규 플랫폼 구축
작성일: 2026-07-03
담당: 홍길동
---
````

- `제목` · `부제` · `사업명` 은 표지에 크게 표시됩니다.
- 그 외의 항목(작성일·담당 등)은 표지 아래 **정보 표**로 정리됩니다.

## F. PDF로 저장 · 인쇄

상단 오른쪽에 **`PDF 저장`** 과 **`인쇄`** 버튼이 나뉘어 있습니다.

- **`PDF 저장`** — 한 번 누르면 **미리보기 그대로** 곧바로 PDF 파일로 저장됩니다. 인쇄 대화상자를 거치지 않고, **저장 위치만 고르면 끝**입니다. (링크·글꼴·다이어그램·수식이 모두 그대로 담깁니다.)
- **`인쇄`** — 실제 프린터로 출력하거나 인쇄 대화상자가 필요할 때 사용합니다.

> 참고: **원본 `.md` 저장**은 `Ctrl+S`(G절), **PDF 내보내기**는 `PDF 저장` 입니다 — 둘은 다른 기능입니다.

## G. 편집기에서 편하게 쓰기

왼쪽 편집기에서 바로 쓸 수 있는 편의 기능입니다.

- **찾기 / 바꾸기** — `Ctrl+F`(찾기), `Ctrl+H`(바꾸기). 일치하는 항목이 모두 강조되고, 개수 표시·다음/이전 이동(`Enter` · `Shift+Enter`)·대소문자 구분을 지원합니다. `변경`으로 현재 항목만, `모두 변경`으로 전체를 한 번에 바꿀 수 있으며 `Ctrl+Z`로 되돌릴 수 있습니다.
- **원본 저장 · 파일 연결** — `Ctrl+S`로 편집 내용을 원본 `.md`에 저장합니다. 파일을 **`MD 파일 열기`·더블클릭**으로 열었으면 바로 덮어쓰기되고, **끌어다 놓기**로 연 경우엔 처음 한 번 저장 위치를 정하면(또는 파일 목록의 `파일 연결`) 이후부터 바로 저장됩니다.
- **자동 저장** — 설정(⚙)에서 켜면, 위처럼 파일이 연결된 상태에서 편집을 멈출 때마다 원본에 자동 저장됩니다.
- **Tab 들여쓰기** — `Tab` / `Shift+Tab`으로 들여쓰기·내어쓰기 (VSCode와 유사).

*이 문서는 MDeautify로 변환되었습니다.*
